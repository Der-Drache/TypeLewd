import { BaseCovering } from './BaseCovering';

export class Liquid extends BaseCovering {

  // TODO amount of liquid

  constructor(name: string) {
    super(name);
  }

  //#region accessors
  //#endregion
}
